# Sesión 12 · Despliegue y evaluación de productos de datos

## Entorno

```bash
uv sync --group deploy
uv run jupyter lab
```

## Contenido

- `assets`

## Pendiente

Sin material propio. Lo más cercano son los `streamlit_app.py` de la sesión 05.

## Datos

Los datasets viven en Hugging Face, no en este repo:

```bash
uv run python scripts/fetch_data.py 12-despliegue-evaluacion
```
