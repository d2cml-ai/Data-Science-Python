# Lecture 2 - Web Scraping (macOS / Windows / Linux)
# Instalar en un entorno virtual:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# NOTA: ya no se necesita webdriver-manager ni descargar chromedriver a mano.
# Selenium >= 4.6 incluye Selenium Manager, que resuelve el driver segun tu
# version de Chrome y tu arquitectura (arm64 / x86_64).

selenium>=4.20
pandas>=2.0
lxml>=5.0
html5lib>=1.1
beautifulsoup4>=4.12
openpyxl>=3.1
Unidecode>=1.3
ipykernel>=6.29
jupyter>=1.1
tqdm>=4.67
